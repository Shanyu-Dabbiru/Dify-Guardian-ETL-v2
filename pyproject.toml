[project]
name = "dify-agentic-heal"
version = "0.1.0"
description = "Self-healing RAG pipeline guard against schema drift in Dify"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.27",
    "pandas>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
